apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frotend.fullname" . }}-test-connection"
  labels:
    {{- include "frotend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frotend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
